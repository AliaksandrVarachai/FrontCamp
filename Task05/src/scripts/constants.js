const NEWS_TYPES = {
    "ordinary": 1,
    "fast": 2,
}

const debug_mode = true;

//let current_news_type = NEWS_TYPES.ordinary;
let current_news_type = NEWS_TYPES.fast;

export {
    NEWS_TYPES,
    current_news_type,
    debug_mode,
}